# Python dependencies for Confluence RAG pipeline
# Install with: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
aiofiles>=23.2.1                 # required by FastAPI StaticFiles

# ── Pydantic ──────────────────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── LangChain core ────────────────────────────────────────────────────────────
langchain>=0.3.25
langchain-core>=0.3.57

# ── LangChain integrations ────────────────────────────────────────────────────
langchain-ollama>=0.3.3          # ChatOllama + OllamaEmbeddings
langchain-postgres>=0.0.13       # PGVector vector store
langchain-mcp-adapters>=0.1.5    # MCP tool adapter (MultiServerMCPClient)

# ── Postgres drivers ──────────────────────────────────────────────────────────
psycopg2-binary>=2.9.10          # sync driver (used by connection pool + langchain-postgres)
pgvector>=0.3.6                  # pgvector Python type support
psycopg2==2.9.11
# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx>=0.28.0                    # used internally by langchain-mcp-adapters
 